\section{The UOR Atlas: An $E_8$ Topological Carrier}

The structural authority of this computer is the UOR Atlas---a 24-dimensional topological carrier $V_T \otimes V_O$ parameterized by scope $q=4$, modality $T=3$, and context $O=8$. 

Unlike ad-hoc logical qubit constructs, the Atlas is embedded deeply into the exceptional $E_8$ root lattice geometry. By anchoring the categorical operations to the $E_8$ Weyl group, the model leverages 96 distinct non-Abelian anyonic classes. These objects map to state-space computations indexing over an inverse-limit $A_\infty$ structural belt comprising 12,288 discrete addresses.

The most profound divergence from classical simulators is the role of memory. Rather than dynamically allocating exponentially growing complex arrays, computations manipulate states across this fixed geometric belt. The fundamental reflections ($\sigma$, $\tau$, $\mu$) act as rigid coordinate permutations over the 96 classes. Because these reflections are pure permutations, they strictly preserve the Euclidean definite companion $\langle x, x \rangle = \sum x_i^2$, providing a mathematically exact unitary operation base without the overhead of floating-point normalization.

This topological base is not a theoretical abstraction; every property---from the Weyl permutations to the $E_8$ seed and the associative composition norms---has been mechanically extracted from the F1 Lean mathematical formalization.
